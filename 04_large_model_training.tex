\section{CGA-LM: Replacing Token Attention with Fixed Concept Attention}
\label{sec:large-model-training}

For language modeling, the world state is a prefix and the consequence is the
next token.
CGA-LM replaces the causal attention sublayer inside a decoder block with
Concept Graph Attention:
\[
\operatorname{CGABlock}(X)
=
X+\operatorname{CGA}(\operatorname{Norm}(X)).
\]
The block has the same external shape contract as attention,
$[B,L,d_{\rm model}]\mapsto[B,L,d_{\rm model}]$.
The surrounding MLP, residual stream, layer normalization, embeddings, and
language-model head are otherwise standard decoder components.

\subsection{Causal concept attention}

At layer $\ell$, token $t$ produces read query, write key, write value, write
gate, and retention-decay projections:
\[
q_t=Q_\ell x_t,\qquad k_t=K_\ell x_t,\qquad v_t=V_\ell x_t,\qquad
g_t=\sigma(w_g^\top x_t+b_g).
\]
The layer-local CGA state is a fixed concept bank
\[
\mathcal{M}_t^\ell
=
\{(a_j,s^k_{jh},s^v_{jh},n_{jh})_{h=1}^{H}\}_{j=1}^{C}.
\]
The read is causal: token $t$ reads only concept contents produced by earlier
writes.
For all concept slots,
\[
\alpha_{thj}
=
\operatorname{softmax}_{j\le C}
\left(\gamma_\ell q_{th}^\top c_{j h}\right),
\qquad
z_{th}=\sum_{j\le C}\alpha_{thj}m_{j h},
\qquad
y_t=O_\ell[z_{t1};\ldots;z_{tH}].
\]
Multi-head CGA uses independent address and value subspaces per head and
concatenates the head outputs before the output projection, exactly as
multi-head attention does.

\subsection{Read before write}

The update order is the same during training and autoregressive inference:
\[
\text{read from }\mathcal{M}_t^\ell
\rightarrow
\text{emit }y_t
\rightarrow
\text{decay and write }(k_t,v_t,g_t)
\rightarrow
\text{update }\mathcal{M}_{t+1}^\ell .
\]
This prevents a token from reading its own write.
The inference-time update is the test-time learning mechanism in CGA-LM:
the model weights are fixed, but the concept bank changes after every causal
block and future tokens condition on that updated bank.
Both MQAR and language-model variants use a learned write gate.
For general text, the update includes a trainable retention factor
$\rho_h=\exp(-\sum_t\lambda_{th})$ over old concept statistics, with the
default PoST-style ordered decay parameterization and an optional free per-head
parameterization for ablation.

\subsection{Training boundary}

Slow neural parameters are trained by ordinary next-token backpropagation
through the CGA read path.
The concept-bank routing state is detached:
\[
\mathcal{M}_{t+1}^\ell
=
\operatorname{CGAWrite}
\left(
\operatorname{stopgrad}(\mathcal{M}_t^\ell),
\operatorname{stopgrad}(k_t),
v_t,
g_t,
\rho_t
\right).
\]
Thus CGA-LM is not a backprop-free language model.
It is an attention replacement whose fast state is updated by a forward-time
concept-memory rule while the slow projections are trained end-to-end.
The gated value content and retention factors remain differentiable through
future reads; routing, key statistics, counts, and slot priors are detached.

\begin{algorithm}[H]
\caption{\textsc{CGALayerForward}}
\label{alg:cga-lm}
\begin{algorithmic}[1]
\State \textbf{Input:} hidden states $x_{1:L}$, fixed concept bank $\mathcal{M}$
\For{causal blocks}
\State project block queries, write keys, and values
\State read all concept slots with continuous attention weights
\State emit block outputs through the output projection
\State form write pairs, write gates, and retention factors
\State route writes to concept slots
\State decay old concept statistics, scatter-add new statistics, and commit bank updates
\EndFor
\State \Return contextualized hidden states
\end{algorithmic}
\end{algorithm}

\subsection{Scaling path}

The reference CGA implementation uses dense concept reads.
This is appropriate for state-equalized MQAR where the concept budget is small.
For larger concept banks, fused dense-read kernels, sparse concept retrieval,
and sharded concept banks are systems work.
The paper therefore separates the architectural claim from the kernel claim:
CGA changes the memory basis of attention from tokens to concepts; optimized
implementations should then choose dense or sparse concept attention depending
on the concept budget.
